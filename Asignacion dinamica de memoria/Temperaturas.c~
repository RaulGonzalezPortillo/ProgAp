#include <stdio.h>
#include <string.h>

int main (void)
{
  int numtemps, *listatemps;
  printf ("Cuantas temepraturas quieres?\n");
  scanf ("%d", &numtemps);
  listatemps = malloc (sizeof (int) * numtemps);
}
